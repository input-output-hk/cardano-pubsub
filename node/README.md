# node

Rust implementation of the `cardano-pubsub` node.

See the [repository README](../README.md) for the project overview, background, and status.

## Build

```bash
cargo build --release
```

## Run

```bash
cargo run --release -- --config <path/to/config.toml>
```

## Test

```bash
cargo test
```
